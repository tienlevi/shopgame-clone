import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2d6ffd",
    },
    error: {
      main: "#ff3232",
    },
    success: {
      main: "#01DF01",
    },
  },
});

export default theme;
